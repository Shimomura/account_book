<%= form_with(model: wallet_history, local: true) do |form| %>
  <% if wallet_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wallet_history.errors.count, "error") %> prohibited this wallet_history from being saved:</h2>

      <ul>
      <% wallet_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
